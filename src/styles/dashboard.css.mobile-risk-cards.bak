.dashboard-page {
  --dashboard-header-offset: var(--app-header-live-h, var(--current-header-h, 82px));
  --dashboard-hero-compact-h: 56px;

  position: relative;
  width: 100%;
  max-width: 100%;
  margin: 0 auto;
  padding: 0;
  color: #0f172a;
  overflow: visible;
}

/* =========================
   HERO — FIXED MERGE BEHAVIOR
========================= */

.dashboard-hero {
  position: sticky;
  top: calc(var(--dashboard-header-offset) + 10px);
  z-index: 900;

  overflow: hidden;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 20px;

  min-height: 132px;
  padding: 30px 32px;
  border-radius: 30px;

  color: #ffffff;
  background:
    radial-gradient(circle at top right, rgba(255, 255, 255, 0.18), transparent 34%),
    linear-gradient(135deg, #123f73 0%, #18599d 56%, #2368b5 100%);
  box-shadow: 0 22px 48px rgba(15, 48, 87, 0.2);

  transform: translateZ(0);
  transition:
    top 0.2s ease,
    min-height 0.2s ease,
    max-height 0.2s ease,
    padding 0.2s ease,
    border-radius 0.2s ease,
    box-shadow 0.2s ease;
}

.dashboard-hero::after {
  content: '';
  position: absolute;
  top: -80px;
  right: -80px;
  width: 240px;
  height: 240px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.11);
  pointer-events: none;
  transition: opacity 0.2s ease;
}

.dashboard-hero > * {
  position: relative;
  z-index: 1;
}

.dashboard-eyebrow,
.dashboard-card-kicker,
.dashboard-modal-eyebrow {
  margin: 0 0 8px;
  font-size: 0.74rem;
  font-weight: 950;
  letter-spacing: 0.14em;
  text-transform: uppercase;
}

.dashboard-eyebrow {
  display: inline-flex;
  color: #fed7aa;
  letter-spacing: 0.16em;
}

.dashboard-hero h1 {
  max-width: 900px;
  margin: 0;
  color: #ffffff;
  font-size: clamp(2.3rem, 6vw, 4rem);
  font-weight: 950;
  line-height: 0.98;
  letter-spacing: -0.045em;
}

.dashboard-hero p:not(.dashboard-eyebrow) {
  max-width: 780px;
  margin: 14px 0 0;
  color: rgba(255, 255, 255, 0.88);
  font-size: 1rem;
  font-weight: 750;
  line-height: 1.55;
}

/* This makes the hero merge with the main header */
.dashboard-page.is-dashboard-scrolled .dashboard-hero {
  position: fixed;
  top: calc(var(--dashboard-header-offset) - 1px);
  left: 0;
  right: 0;
  z-index: 899;

  width: 100%;
  max-width: none;
  min-height: var(--dashboard-hero-compact-h);
  max-height: var(--dashboard-hero-compact-h);
  margin: 0;
  padding: 0 20px;
  border-radius: 0;

  display: flex;
  align-items: center;
  justify-content: flex-start;

  box-shadow: none;
}

.dashboard-page.is-dashboard-scrolled {
  padding-top: var(--dashboard-hero-compact-h);
}

.dashboard-page.is-dashboard-scrolled .dashboard-hero::after,
.dashboard-page.is-dashboard-scrolled .dashboard-eyebrow,
.dashboard-page.is-dashboard-scrolled .dashboard-hero p:not(.dashboard-eyebrow) {
  display: none;
}

.dashboard-page.is-dashboard-scrolled .dashboard-hero h1 {
  max-width: 100%;
  overflow: hidden;
  margin: 0;
  color: #ffffff;
  font-size: clamp(1.08rem, 4vw, 1.38rem);
  font-weight: 950;
  line-height: 1.1;
  letter-spacing: -0.018em;
  white-space: nowrap;
  text-overflow: ellipsis;
}

/* =========================
   6 SMALL DASHBOARD CARDS
========================= */

.dashboard-stat-grid {
  display: grid;
  grid-template-columns: repeat(6, minmax(0, 1fr));
  gap: 14px;
  margin-top: 22px;
}

.dashboard-stat-card,
.dashboard-project-list button,
.dashboard-legend-list button {
  font-family: inherit;
}

.dashboard-stat-card {
  position: relative;
  overflow: hidden;
  min-height: 118px;
  border: 1px solid rgba(23, 75, 135, 0.11);
  border-top: 5px solid #174b87;
  border-radius: 22px;
  padding: 16px 14px;
  cursor: pointer;
  text-align: left;
  color: #0f172a;
  background: rgba(255, 255, 255, 0.96);
  box-shadow: 0 16px 36px rgba(15, 48, 87, 0.08);
  transition:
    transform 180ms ease,
    box-shadow 180ms ease,
    border-color 180ms ease;
}

.dashboard-stat-card::after {
  content: '';
  position: absolute;
  right: -30px;
  bottom: -30px;
  width: 86px;
  height: 86px;
  border-radius: 999px;
  background: #174b87;
  opacity: 0.08;
}

.dashboard-stat-card:hover,
.dashboard-project-list button:hover,
.dashboard-legend-list button:hover {
  transform: translateY(-2px);
  box-shadow: 0 18px 42px rgba(15, 48, 87, 0.13);
}

.dashboard-stat-card:focus-visible,
.dashboard-project-list button:focus-visible,
.dashboard-legend-list button:focus-visible,
.dashboard-modal-close:focus-visible,
.dashboard-modal-view-btn:focus-visible {
  outline: 3px solid rgba(249, 115, 22, 0.32);
  outline-offset: 3px;
}

.dashboard-stat-card.total {
  border-top-color: #174b87;
}

.dashboard-stat-card.ongoing {
  border-top-color: #16a34a;
}

.dashboard-stat-card.ongoing::after {
  background: #16a34a;
}

.dashboard-stat-card.completed {
  border-top-color: #2563eb;
}

.dashboard-stat-card.completed::after {
  background: #2563eb;
}

.dashboard-stat-card.not-started {
  border-top-color: #64748b;
}

.dashboard-stat-card.not-started::after {
  background: #64748b;
}

.dashboard-stat-card.high-risk {
  border-top-color: #ef4444;
}

.dashboard-stat-card.high-risk::after {
  background: #ef4444;
}

.dashboard-stat-card.for-review {
  border-top-color: #f97316;
}

.dashboard-stat-card.for-review::after {
  background: #f97316;
}

.dashboard-stat-card span {
  position: relative;
  z-index: 1;
  display: block;
  color: #64748b;
  font-size: 0.72rem;
  font-weight: 950;
  letter-spacing: 0.12em;
  line-height: 1.15;
  text-transform: uppercase;
}

.dashboard-stat-card strong {
  position: relative;
  z-index: 1;
  display: block;
  margin-top: 12px;
  color: #0f172a;
  font-size: clamp(1.5rem, 2.8vw, 2rem);
  font-weight: 950;
  line-height: 1;
  letter-spacing: -0.05em;
}

.dashboard-stat-card small {
  position: relative;
  z-index: 1;
  display: block;
  margin-top: 9px;
  color: #94a3b8;
  font-size: 0.74rem;
  font-weight: 850;
  line-height: 1.2;
}

/* =========================
   MAIN DASHBOARD SECTIONS
========================= */

.dashboard-priority-section,
.dashboard-recent-section {
  margin-top: 20px;
}

.dashboard-main-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.dashboard-chart-row {
  align-items: stretch;
}

.dashboard-chart-card,
.dashboard-list-card {
  min-width: 0;
  overflow: hidden;
  border: 1px solid rgba(226, 232, 240, 0.95);
  border-radius: 28px;
  background: #ffffff;
  box-shadow: 0 16px 45px rgba(15, 23, 42, 0.08);
}

.dashboard-card-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 14px;
  padding: 22px 22px 12px;
}

.dashboard-card-header h2 {
  margin: 0;
  color: #0f172a;
  font-size: 1.12rem;
  line-height: 1.2;
  letter-spacing: -0.025em;
}

.dashboard-card-kicker {
  color: #f97316;
}

.dashboard-card-header > span {
  flex: 0 0 auto;
  border-radius: 999px;
  padding: 8px 11px;
  color: #1d4ed8;
  background: #eff6ff;
  font-size: 0.78rem;
  font-weight: 900;
}

.dashboard-card-header button {
  border: 0;
  border-radius: 999px;
  padding: 9px 13px;
  cursor: pointer;
  color: #ffffff;
  background: #f97316;
  font-size: 0.82rem;
  font-weight: 900;
  box-shadow: 0 10px 24px rgba(249, 115, 22, 0.24);
  transition:
    transform 180ms ease,
    box-shadow 180ms ease,
    background 180ms ease;
}

.dashboard-card-header button:hover {
  transform: translateY(-1px);
  background: #ea580c;
  box-shadow: 0 14px 34px rgba(249, 115, 22, 0.28);
}

.dashboard-chart-area {
  height: 210px;
  padding: 2px 12px 8px;
}

.dashboard-legend-list {
  display: grid;
  gap: 8px;
  padding: 0 18px 20px;
}

.dashboard-legend-list button {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr) auto;
  gap: 10px;
  align-items: center;
  border: 0;
  border-radius: 16px;
  padding: 11px 12px;
  cursor: pointer;
  text-align: left;
  background: #f8fafc;
  transition:
    transform 180ms ease,
    box-shadow 180ms ease,
    background 180ms ease;
}

.dashboard-legend-list button:hover {
  background: #ffffff;
}

.dashboard-legend-list i {
  width: 10px;
  height: 10px;
  border-radius: 999px;
}

.dashboard-legend-list span {
  overflow: hidden;
  color: #334155;
  font-size: 0.88rem;
  font-weight: 850;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.dashboard-legend-list strong {
  color: #0f172a;
  font-size: 0.9rem;
  font-weight: 950;
}

.dashboard-project-list {
  display: grid;
  gap: 10px;
  padding: 0 20px 22px;
}

.dashboard-project-list button {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 14px;
  align-items: center;
  border: 0;
  border-radius: 18px;
  padding: 15px;
  cursor: pointer;
  text-align: left;
  background: #f8fafc;
  transition:
    transform 180ms ease,
    box-shadow 180ms ease,
    background 180ms ease;
}

.dashboard-project-list button:hover {
  background: #ffffff;
}

.dashboard-project-list strong {
  display: block;
  overflow: hidden;
  color: #0f172a;
  font-size: 0.95rem;
  font-weight: 950;
  line-height: 1.25;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.dashboard-project-list span {
  display: block;
  overflow: hidden;
  margin-top: 5px;
  color: #64748b;
  font-size: 0.84rem;
  font-weight: 750;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.dashboard-project-list em {
  color: #1d4ed8;
  font-size: 0.82rem;
  font-style: normal;
  font-weight: 950;
  white-space: nowrap;
}

.dashboard-progress-label {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  margin-bottom: 8px;
}

.dashboard-progress-label span {
  color: #64748b;
  font-size: 0.78rem;
  font-weight: 900;
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

.dashboard-progress-label strong {
  color: #0f172a;
  font-size: 0.9rem;
  font-weight: 950;
}

.dashboard-progress-track {
  width: 100%;
  height: 9px;
  overflow: hidden;
  border-radius: 999px;
  background: #e2e8f0;
}

.dashboard-progress-track span {
  display: block;
  height: 100%;
  min-width: 0;
  max-width: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, #1d4ed8, #2563eb);
  transition: width 240ms ease;
}

.dashboard-progress-track.financial span {
  background: linear-gradient(90deg, #f97316, #fb923c);
}

/* =========================
   STATES
========================= */

.dashboard-empty-state,
.dashboard-loading-card,
.dashboard-error-card {
  padding: 28px;
  border: 1px solid rgba(226, 232, 240, 0.95);
  border-radius: 28px;
  background: #ffffff;
  color: #64748b;
  text-align: center;
  font-weight: 800;
  box-shadow: 0 16px 45px rgba(15, 23, 42, 0.08);
}

.dashboard-empty-state.compact {
  min-height: 120px;
  display: grid;
  place-items: center;
  box-shadow: none;
}

.dashboard-empty-state strong,
.dashboard-loading-card h2,
.dashboard-error-card h2 {
  display: block;
  margin: 0;
  color: #0f172a;
  font-size: 1.05rem;
  font-weight: 950;
}

.dashboard-empty-state p,
.dashboard-loading-card p,
.dashboard-error-card p {
  max-width: 360px;
  margin: 8px auto 0;
  color: #64748b;
  font-size: 0.92rem;
  line-height: 1.55;
}

.dashboard-loading-card,
.dashboard-error-card {
  display: flex;
  align-items: center;
  gap: 18px;
  min-height: 220px;
  text-align: left;
}

.dashboard-error-card {
  display: block;
}

.dashboard-error-card button {
  margin-top: 18px;
  border: 0;
  border-radius: 999px;
  padding: 11px 16px;
  cursor: pointer;
  color: #ffffff;
  background: #f97316;
  font-weight: 900;
}

.dashboard-loader {
  width: 42px;
  height: 42px;
  flex: 0 0 auto;
  border: 4px solid #e2e8f0;
  border-top-color: #1d4ed8;
  border-radius: 999px;
  animation: dashboard-spin 800ms linear infinite;
}

/* =========================
   DRILLDOWN MODAL
========================= */

.dashboard-modal-backdrop {
  position: fixed;
  inset: 0;
  z-index: 2147482500;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 18px;
  background: rgba(15, 23, 42, 0.58);
  backdrop-filter: blur(8px);
  animation: dashboard-modal-backdrop-in 220ms ease;
}

.dashboard-drilldown-modal {
  width: min(980px, 100%);
  max-height: min(86vh, 780px);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  border-radius: 28px;
  background: #ffffff;
  box-shadow: 0 26px 90px rgba(15, 23, 42, 0.32);
  animation: dashboard-modal-in 240ms cubic-bezier(0.22, 1, 0.36, 1);
}

.dashboard-modal-backdrop.is-closing {
  animation: dashboard-modal-backdrop-out 190ms ease forwards;
}

.dashboard-drilldown-modal.is-closing {
  animation: dashboard-modal-out 190ms ease forwards;
}

.dashboard-modal-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
  padding: 22px 22px 18px;
  color: #ffffff;
  background:
    radial-gradient(circle at top right, rgba(255, 255, 255, 0.2), transparent 34%),
    linear-gradient(135deg, #123f73 0%, #18599d 58%, #2368b5 100%);
}

.dashboard-modal-header h2 {
  margin: 0;
  font-size: clamp(1.35rem, 2.6vw, 2rem);
  font-weight: 950;
  line-height: 1.12;
  letter-spacing: -0.035em;
}

.dashboard-modal-header p:not(.dashboard-modal-eyebrow) {
  margin: 8px 0 0;
  color: rgba(255, 255, 255, 0.84);
  line-height: 1.55;
}

.dashboard-modal-close {
  width: 42px;
  height: 42px;
  flex: 0 0 auto;
  border: 0;
  border-radius: 999px;
  cursor: pointer;
  color: #ffffff;
  background: rgba(255, 255, 255, 0.16);
  font-size: 1.8rem;
  line-height: 1;
  transition:
    transform 180ms ease,
    background 180ms ease;
}

.dashboard-modal-close:hover {
  transform: rotate(6deg) scale(1.04);
  background: rgba(255, 255, 255, 0.24);
}

.dashboard-modal-body {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  padding: 16px;
  background: #f8fafc;
}

.dashboard-modal-project-card {
  border: 1px solid rgba(226, 232, 240, 0.95);
  border-radius: 22px;
  background: #ffffff;
  box-shadow: 0 12px 32px rgba(15, 23, 42, 0.07);
  padding: 18px;
  animation: dashboard-project-card-in 220ms ease;
}

.dashboard-modal-project-card + .dashboard-modal-project-card {
  margin-top: 14px;
}

.dashboard-modal-project-main {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 16px;
  align-items: flex-start;
}

.dashboard-modal-project-kicker {
  margin: 0 0 6px;
  color: #f97316;
  font-size: 0.76rem;
  font-weight: 950;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.dashboard-modal-project-main h3 {
  margin: 0;
  color: #0f172a;
  font-size: 1.05rem;
  font-weight: 950;
  line-height: 1.28;
  letter-spacing: -0.02em;
}

.dashboard-modal-project-location {
  margin: 7px 0 0;
  color: #64748b;
  font-size: 0.9rem;
  line-height: 1.45;
}

.dashboard-modal-view-btn {
  border: 0;
  border-radius: 999px;
  padding: 10px 16px;
  cursor: pointer;
  color: #ffffff;
  background: #f97316;
  font-weight: 950;
  white-space: nowrap;
  box-shadow: 0 12px 26px rgba(249, 115, 22, 0.24);
  transition:
    transform 180ms ease,
    box-shadow 180ms ease,
    background 180ms ease;
}

.dashboard-modal-view-btn:hover {
  transform: translateY(-1px);
  background: #ea580c;
  box-shadow: 0 16px 34px rgba(249, 115, 22, 0.3);
}

.dashboard-modal-view-btn:disabled {
  cursor: not-allowed;
  opacity: 0.55;
  transform: none;
  box-shadow: none;
}

.dashboard-modal-project-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
  margin-top: 16px;
}

.dashboard-modal-project-grid div {
  min-width: 0;
  padding: 12px;
  border-radius: 16px;
  background: #f8fafc;
}

.dashboard-modal-project-grid span {
  display: block;
  color: #64748b;
  font-size: 0.7rem;
  font-weight: 950;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.dashboard-modal-project-grid strong {
  display: block;
  overflow: hidden;
  margin-top: 6px;
  color: #0f172a;
  font-size: 0.84rem;
  font-weight: 900;
  line-height: 1.35;
  text-overflow: ellipsis;
}

.dashboard-modal-progress-row {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
  margin-top: 16px;
}

@keyframes dashboard-spin {
  to {
    transform: rotate(360deg);
  }
}

@keyframes dashboard-project-card-in {
  from {
    opacity: 0;
    transform: translateY(8px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes dashboard-modal-backdrop-in {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

@keyframes dashboard-modal-backdrop-out {
  from {
    opacity: 1;
  }

  to {
    opacity: 0;
  }
}

@keyframes dashboard-modal-in {
  from {
    opacity: 0;
    transform: translateY(12px) scale(0.98);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes dashboard-modal-out {
  from {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  to {
    opacity: 0;
    transform: translateY(10px) scale(0.98);
  }
}

/* =========================
   MOBILE-FIRST TUNING
========================= */

@media (max-width: 1180px) {
  .dashboard-stat-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

@media (max-width: 768px) {
  .dashboard-hero {
    top: calc(var(--dashboard-header-offset) + 8px);
    min-height: 124px;
    padding: 24px 22px;
    border-radius: 28px;
  }

  .dashboard-hero h1 {
    font-size: clamp(2.05rem, 9vw, 3rem);
    letter-spacing: -0.04em;
  }

  .dashboard-hero p:not(.dashboard-eyebrow) {
    font-size: 0.92rem;
    line-height: 1.5;
  }

  .dashboard-page.is-dashboard-scrolled {
    padding-top: 52px;
  }

  .dashboard-page.is-dashboard-scrolled .dashboard-hero {
    top: calc(var(--dashboard-header-offset) - 1px);
    min-height: 52px;
    max-height: 52px;
    padding: 0 16px;
    border-radius: 0;
  }

  .dashboard-page.is-dashboard-scrolled .dashboard-hero h1 {
    font-size: 1.12rem;
  }

  .dashboard-stat-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
    margin-top: 16px;
  }

  .dashboard-stat-card {
    min-height: 94px;
    padding: 12px 10px;
    border-radius: 18px;
  }

  .dashboard-stat-card span {
    font-size: 0.6rem;
    letter-spacing: 0.09em;
  }

  .dashboard-stat-card strong {
    margin-top: 10px;
    font-size: clamp(1.05rem, 5vw, 1.42rem);
  }

  .dashboard-stat-card small {
    margin-top: 8px;
    font-size: 0.62rem;
  }

  .dashboard-priority-section,
  .dashboard-recent-section,
  .dashboard-main-grid {
    margin-top: 16px;
  }

  .dashboard-main-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
  }

  .dashboard-chart-card,
  .dashboard-list-card {
    border-radius: 24px;
  }

  .dashboard-chart-card {
    border-radius: 20px;
  }

  .dashboard-card-header {
    padding: 16px 16px 8px;
  }

  .dashboard-chart-card .dashboard-card-header {
    display: block;
    padding: 14px 12px 6px;
  }

  .dashboard-card-header h2 {
    font-size: 1.02rem;
  }

  .dashboard-chart-card .dashboard-card-header h2 {
    font-size: 0.82rem;
    line-height: 1.15;
  }

  .dashboard-chart-card .dashboard-card-kicker {
    margin-bottom: 5px;
    font-size: 0.62rem;
  }

  .dashboard-chart-card .dashboard-card-header > span {
    display: inline-flex;
    margin-top: 8px;
    padding: 6px 8px;
    font-size: 0.64rem;
  }

  .dashboard-chart-area {
    height: 136px;
    padding: 0 4px 4px;
  }

  .dashboard-legend-list {
    gap: 6px;
    padding: 0 10px 12px;
  }

  .dashboard-legend-list button {
    grid-template-columns: auto minmax(0, 1fr) auto;
    gap: 6px;
    border-radius: 12px;
    padding: 8px 8px;
  }

  .dashboard-legend-list i {
    width: 8px;
    height: 8px;
  }

  .dashboard-legend-list span,
  .dashboard-legend-list strong {
    font-size: 0.72rem;
  }

  .dashboard-project-list {
    padding: 0 16px 18px;
  }

  .dashboard-modal-backdrop {
    align-items: center;
    padding: 12px;
  }

  .dashboard-drilldown-modal {
    width: 100%;
    max-height: 88vh;
    border-radius: 24px;
  }

  .dashboard-modal-header {
    padding: 20px;
  }

  .dashboard-modal-project-main {
    grid-template-columns: 1fr;
  }

  .dashboard-modal-view-btn {
    width: 100%;
  }

  .dashboard-modal-project-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .dashboard-modal-progress-row {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 390px) {
  .dashboard-hero {
    padding: 22px 20px;
  }

  .dashboard-hero h1 {
    font-size: 2.05rem;
  }

  .dashboard-page.is-dashboard-scrolled {
    padding-top: 52px;
  }

  .dashboard-page.is-dashboard-scrolled .dashboard-hero h1 {
    font-size: 1.02rem;
  }

  .dashboard-stat-grid {
    gap: 8px;
  }

  .dashboard-stat-card {
    min-height: 88px;
    padding: 11px 8px;
  }

  .dashboard-stat-card span {
    font-size: 0.56rem;
  }

  .dashboard-stat-card strong {
    font-size: 1.04rem;
  }

  .dashboard-stat-card small {
    font-size: 0.58rem;
  }

  .dashboard-main-grid {
    gap: 8px;
  }

  .dashboard-chart-area {
    height: 126px;
  }

  .dashboard-chart-card .dashboard-card-header {
    padding: 12px 10px 4px;
  }

  .dashboard-chart-card .dashboard-card-header h2 {
    font-size: 0.76rem;
  }

  .dashboard-legend-list {
    padding: 0 8px 10px;
  }

  .dashboard-legend-list span,
  .dashboard-legend-list strong {
    font-size: 0.68rem;
  }

  .dashboard-modal-project-grid {
    grid-template-columns: 1fr;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 1ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 1ms !important;
  }
}

/* =========================
   VARIANCE / SLIPPAGE
========================= */
.dashboard-variance-value,
.dashboard-slippage-em {
  font-weight: 950;
  white-space: nowrap;
}

.dashboard-variance-value.behind,
.dashboard-slippage-em.behind {
  color: #b91c1c !important;
}

.dashboard-variance-value.ahead,
.dashboard-slippage-em.ahead {
  color: #15803d !important;
}

.dashboard-variance-value.on-track,
.dashboard-slippage-em.on-track {
  color: #0f477f !important;
}

/* =========================================================
   FINAL STANDARD RISK COLORS — DILG PDMU
   None / No Risk = Green
   Low = Yellow
   Moderate = Orange
   High = Red
   Dashboard visual badges/cards only. Mobile-safe override.
========================================================= */
.dashboard-stat-card.no-risk,
.dashboard-stat-card.none-risk,
.dashboard-stat-card.neutral-risk {
  border-top-color: #16a34a !important;
}

.dashboard-stat-card.no-risk::after,
.dashboard-stat-card.none-risk::after,
.dashboard-stat-card.neutral-risk::after {
  background: #16a34a !important;
}

.dashboard-stat-card.low-risk {
  border-top-color: #facc15 !important;
}

.dashboard-stat-card.low-risk::after {
  background: #facc15 !important;
}

.dashboard-stat-card.moderate-risk,
.dashboard-stat-card.medium-risk,
.dashboard-stat-card.for-review {
  border-top-color: #f97316 !important;
}

.dashboard-stat-card.moderate-risk::after,
.dashboard-stat-card.medium-risk::after,
.dashboard-stat-card.for-review::after {
  background: #f97316 !important;
}

.dashboard-stat-card.high-risk,
.dashboard-stat-card.critical-risk {
  border-top-color: #ef4444 !important;
}

.dashboard-stat-card.high-risk::after,
.dashboard-stat-card.critical-risk::after {
  background: #ef4444 !important;
}

.dashboard-risk-none,
.dashboard-risk-no-risk,
.dashboard-risk-neutral,
.dashboard-risk-unknown,
.dashboard-modal-risk.none,
.dashboard-modal-risk.no-risk,
.dashboard-modal-risk.neutral,
.dashboard-modal-risk.unknown {
  color: #166534 !important;
  background: #dcfce7 !important;
  border-color: #bbf7d0 !important;
}

.dashboard-risk-low,
.dashboard-modal-risk.low {
  color: #854d0e !important;
  background: #fef9c3 !important;
  border-color: #fde68a !important;
}

.dashboard-risk-medium,
.dashboard-risk-moderate,
.dashboard-modal-risk.medium,
.dashboard-modal-risk.moderate {
  color: #c2410c !important;
  background: #ffedd5 !important;
  border-color: #fed7aa !important;
}

.dashboard-risk-high,
.dashboard-risk-critical,
.dashboard-modal-risk.high,
.dashboard-modal-risk.critical {
  color: #b91c1c !important;
  background: #fee2e2 !important;
  border-color: #fecaca !important;
}



/* =========================================================
   FINAL DASHBOARD PRIORITY + DRILLDOWN MOBILE FIX
   - Priority Review list scrolls inside the card.
   - Drilldown modal starts below the fixed DILG main banner.
   - Drilldown body scrolls internally.
========================================================= */
.dashboard-priority-section .dashboard-list-card {
  display: flex;
  flex-direction: column;
  max-height: 430px;
}

.dashboard-priority-section .dashboard-card-header {
  flex: 0 0 auto;
}

.dashboard-priority-section .dashboard-project-list {
  flex: 1 1 auto;
  min-height: 0;
  max-height: 286px;
  overflow-y: auto;
  overscroll-behavior: contain;
  padding-right: 14px;
  scrollbar-width: thin;
}

.dashboard-priority-section .dashboard-project-list::-webkit-scrollbar,
.dashboard-modal-body::-webkit-scrollbar {
  width: 8px;
}

.dashboard-priority-section .dashboard-project-list::-webkit-scrollbar-track,
.dashboard-modal-body::-webkit-scrollbar-track {
  background: rgba(226, 232, 240, 0.6);
  border-radius: 999px;
}

.dashboard-priority-section .dashboard-project-list::-webkit-scrollbar-thumb,
.dashboard-modal-body::-webkit-scrollbar-thumb {
  background: rgba(100, 116, 139, 0.55);
  border-radius: 999px;
}

.dashboard-modal-backdrop {
  --dashboard-modal-top-offset: calc(var(--app-header-live-h, var(--current-header-h, 82px)) + 10px);

  inset: var(--dashboard-modal-top-offset) 0 0;
  align-items: flex-start;
  justify-content: center;
  padding: 14px 18px 18px;
}

.dashboard-drilldown-modal {
  max-height: calc(100dvh - var(--app-header-live-h, var(--current-header-h, 82px)) - 34px);
}

.dashboard-modal-header {
  flex: 0 0 auto;
}

.dashboard-modal-body {
  flex: 1 1 auto;
  min-height: 0;
  overflow-y: auto;
  overscroll-behavior: contain;
}

@supports not (height: 100dvh) {
  .dashboard-drilldown-modal {
    max-height: calc(100vh - var(--app-header-live-h, var(--current-header-h, 82px)) - 34px);
  }
}

@media (max-width: 768px) {
  .dashboard-priority-section .dashboard-list-card {
    max-height: 390px;
  }

  .dashboard-priority-section .dashboard-project-list {
    max-height: 248px;
    padding-right: 10px;
  }

  .dashboard-modal-backdrop {
    --dashboard-modal-top-offset: calc(var(--app-header-live-h, var(--current-header-h, 74px)) + 8px);

    align-items: flex-start;
    padding: 10px 10px 14px;
  }

  .dashboard-drilldown-modal {
    width: 100%;
    max-height: calc(100dvh - var(--app-header-live-h, var(--current-header-h, 74px)) - 24px);
    border-radius: 22px;
  }

  .dashboard-modal-header {
    padding: 16px 16px 14px;
  }

  .dashboard-modal-header h2 {
    font-size: clamp(1.12rem, 5.2vw, 1.55rem);
  }

  .dashboard-modal-header p:not(.dashboard-modal-eyebrow) {
    margin-top: 6px;
    font-size: 0.9rem;
    line-height: 1.45;
  }

  .dashboard-modal-close {
    width: 40px;
    height: 40px;
    font-size: 1.6rem;
  }

  .dashboard-modal-body {
    padding: 12px;
  }
}

@media (max-width: 390px) {
  .dashboard-priority-section .dashboard-list-card {
    max-height: 372px;
  }

  .dashboard-priority-section .dashboard-project-list {
    max-height: 236px;
  }

  .dashboard-drilldown-modal {
    max-height: calc(100dvh - var(--app-header-live-h, var(--current-header-h, 74px)) - 20px);
    border-radius: 20px;
  }

  .dashboard-modal-header {
    padding: 14px;
  }
}

/* =========================================================
   FINAL REQUEST — SHORTER DASHBOARD DRILLDOWN WINDOW
   - Header position remains below the DILG main banner.
   - Lower project list area is shortened so only one project is initially visible.
   - Additional projects scroll inside the lower section.
========================================================= */
.dashboard-drilldown-modal {
  max-height: min(
    64dvh,
    calc(100dvh - var(--app-header-live-h, var(--current-header-h, 82px)) - 58px),
    560px
  ) !important;
}

.dashboard-modal-body {
  flex: 0 1 auto !important;
  max-height: 320px !important;
  overflow-y: auto !important;
  overscroll-behavior: contain !important;
  scrollbar-gutter: stable !important;
}

.dashboard-modal-project-card {
  scroll-margin-top: 12px !important;
}

@supports not (height: 100dvh) {
  .dashboard-drilldown-modal {
    max-height: min(
      64vh,
      calc(100vh - var(--app-header-live-h, var(--current-header-h, 82px)) - 58px),
      560px
    ) !important;
  }
}

@media (max-width: 768px) {
  .dashboard-drilldown-modal {
    max-height: min(
      66dvh,
      calc(100dvh - var(--app-header-live-h, var(--current-header-h, 74px)) - 44px),
      500px
    ) !important;
  }

  .dashboard-modal-body {
    max-height: 286px !important;
    padding: 10px !important;
  }

  .dashboard-modal-project-card {
    padding: 14px !important;
    border-radius: 18px !important;
  }

  .dashboard-modal-project-card + .dashboard-modal-project-card {
    margin-top: 10px !important;
  }

  .dashboard-modal-project-grid {
    gap: 8px !important;
    margin-top: 12px !important;
  }

  .dashboard-modal-project-grid div {
    padding: 10px !important;
    border-radius: 13px !important;
  }

  .dashboard-modal-progress-row {
    gap: 10px !important;
    margin-top: 12px !important;
  }
}

@media (max-width: 390px) {
  .dashboard-drilldown-modal {
    max-height: min(
      68dvh,
      calc(100dvh - var(--app-header-live-h, var(--current-header-h, 74px)) - 36px),
      470px
    ) !important;
  }

  .dashboard-modal-body {
    max-height: 262px !important;
  }
}


/* =========================================================
   FINAL REQUEST V2 — SLIGHTLY TALLER DASHBOARD DRILLDOWN
   - Keeps the header below the fixed DILG banner.
   - Makes the modal body taller than V1 while still scrollable.
   - Shows roughly one full project plus a hint of the next item on phones.
========================================================= */
.dashboard-drilldown-modal {
  max-height: min(
    72dvh,
    calc(100dvh - var(--app-header-live-h, var(--current-header-h, 82px)) - 44px),
    640px
  ) !important;
}

.dashboard-modal-body {
  max-height: 390px !important;
}

@supports not (height: 100dvh) {
  .dashboard-drilldown-modal {
    max-height: min(
      72vh,
      calc(100vh - var(--app-header-live-h, var(--current-header-h, 82px)) - 44px),
      640px
    ) !important;
  }
}

@media (max-width: 768px) {
  .dashboard-drilldown-modal {
    max-height: min(
      74dvh,
      calc(100dvh - var(--app-header-live-h, var(--current-header-h, 74px)) - 34px),
      560px
    ) !important;
  }

  .dashboard-modal-body {
    max-height: 350px !important;
  }
}

@media (max-width: 390px) {
  .dashboard-drilldown-modal {
    max-height: min(
      75dvh,
      calc(100dvh - var(--app-header-live-h, var(--current-header-h, 74px)) - 30px),
      530px
    ) !important;
  }

  .dashboard-modal-body {
    max-height: 326px !important;
  }
}

/* =========================================================
   FINAL REQUEST V3 — TALLER AND LOWER DASHBOARD DRILLDOWN
   - Moves the modal lower by centering it within the area below the fixed DILG header.
   - Makes the modal taller than V2 while keeping internal scrolling.
   - Header remains safely below the main banner.
========================================================= */
.dashboard-modal-backdrop {
  --dashboard-modal-top-offset: calc(var(--app-header-live-h, var(--current-header-h, 82px)) + 10px);
  inset: var(--dashboard-modal-top-offset) 0 0 !important;
  align-items: center !important;
  justify-content: center !important;
  padding: 18px !important;
}

.dashboard-drilldown-modal {
  width: min(980px, calc(100vw - 20px)) !important;
  max-height: min(
    82dvh,
    calc(100dvh - var(--app-header-live-h, var(--current-header-h, 82px)) - 42px),
    720px
  ) !important;
}

.dashboard-modal-body {
  flex: 1 1 auto !important;
  max-height: 500px !important;
  overflow-y: auto !important;
  overscroll-behavior: contain !important;
  scrollbar-gutter: stable !important;
}

@supports not (height: 100dvh) {
  .dashboard-drilldown-modal {
    max-height: min(
      82vh,
      calc(100vh - var(--app-header-live-h, var(--current-header-h, 82px)) - 42px),
      720px
    ) !important;
  }
}

@media (max-width: 768px) {
  .dashboard-modal-backdrop {
    --dashboard-modal-top-offset: calc(var(--app-header-live-h, var(--current-header-h, 74px)) + 8px);
    align-items: center !important;
    padding: 14px 8px 16px !important;
  }

  .dashboard-drilldown-modal {
    width: calc(100vw - 16px) !important;
    max-height: min(
      80dvh,
      calc(100dvh - var(--app-header-live-h, var(--current-header-h, 74px)) - 36px),
      640px
    ) !important;
    border-radius: 22px !important;
  }

  .dashboard-modal-body {
    max-height: 430px !important;
    padding: 12px !important;
  }
}

@media (max-width: 390px) {
  .dashboard-drilldown-modal {
    max-height: min(
      79dvh,
      calc(100dvh - var(--app-header-live-h, var(--current-header-h, 74px)) - 34px),
      610px
    ) !important;
  }

  .dashboard-modal-body {
    max-height: 404px !important;
  }
}


/* =========================
   PMS10 DASHBOARD 9-CARD LAYOUT
   5 cards top row, 4 wider cards bottom row
========================= */

@media (min-width: 1180px) {
  .dashboard-stat-grid {
    display: grid !important;
    grid-template-columns: repeat(20, minmax(0, 1fr)) !important;
    gap: 14px !important;
    align-items: stretch !important;
  }

  .dashboard-stat-grid .dashboard-stat-card:nth-child(-n + 5) {
    grid-column: span 4 !important;
  }

  .dashboard-stat-grid .dashboard-stat-card:nth-child(n + 6) {
    grid-column: span 5 !important;
  }
}

@media (min-width: 760px) and (max-width: 1179px) {
  .dashboard-stat-grid {
    display: grid !important;
    grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
    gap: 12px !important;
  }

  .dashboard-stat-grid .dashboard-stat-card {
    grid-column: auto !important;
  }
}

@media (max-width: 759px) {
  .dashboard-stat-grid {
    display: grid !important;
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
    gap: 10px !important;
  }

  .dashboard-stat-grid .dashboard-stat-card {
    grid-column: auto !important;
  }
}

@media (max-width: 420px) {
  .dashboard-stat-grid {
    grid-template-columns: 1fr !important;
  }
}

.dashboard-stat-card.under-procurement {
  border-top-color: #f97316 !important;
}

.dashboard-stat-card.not-started {
  border-top-color: #64748b !important;
}

.dashboard-stat-card.ongoing {
  border-top-color: #16a34a !important;
}

.dashboard-stat-card.completed {
  border-top-color: #2563eb !important;
}

.dashboard-stat-card.critical-status {
  border-top-color: #ef4444 !important;
}

.dashboard-stat-card.low-risk {
  border-top-color: #eab308 !important;
}

.dashboard-stat-card.medium-risk {
  border-top-color: #f97316 !important;
}

.dashboard-stat-card.high-risk {
  border-top-color: #ef4444 !important;
}

/* =========================
   PMS10 RESCUE RESTORE MERGE
   Safe dashboard polish only. Original hero merge behavior is preserved.
========================= */

@media (min-width: 901px) {
  .dashboard-stat-card,
  .dashboard-chart-card,
  .dashboard-panel,
  .dashboard-card,
  .dashboard-priority-card {
    border-radius: 22px;
    box-shadow: var(--pms10-card-shadow, 0 12px 32px rgba(15, 23, 42, 0.07));
    border-color: var(--pms10-card-border, rgba(148, 163, 184, 0.18));
  }

  .dashboard-stat-grid {
    gap: 14px;
  }
}
